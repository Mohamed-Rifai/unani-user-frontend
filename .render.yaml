services:
  - type: web
    name: frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    repo: https://github.com/Mohamed-Rifai/unani-user-frontend
    branch: main
    runtime: node
    healthCheckPath: /
    autoDeploy: true
